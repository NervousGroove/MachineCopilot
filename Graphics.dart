// Import the graphics library
import 'graphics.dart';

// Create a graphics object
Graphics graphics = new Graphics();

// Set the quality to maximum
graphics.setQuality(Quality.maximum);

// Set the renderer to WebGL
graphics.setRenderer(Renderer.webgl);